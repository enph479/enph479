<Window x:Class="ElectricalToolSuite.ScheduleImport.UI.ManageScheduleLinksDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:ElectricalToolSuite.ScheduleImport.UI"
             mc:Ignorable="d" 
             d:DesignHeight="467" d:DesignWidth="674" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Width="800" Height="450"
        d:DataContext="{d:DesignInstance ui:ManageScheduleLinksDialog}">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" CanExecute="CommandBinding_CanExecute"/>
    </Window.CommandBindings>
    <Grid>
        <DataGrid x:Name="ManagedScheduleLinksDataGrid" Margin="30,30,30,100" SelectionMode="Single" AutoGenerateColumns="True" SelectedCellsChanged="ManagedScheduleLinksDataGrid_SelectedCellsChanged"/>
        <Button x:Name="ReloadButton" Content="Reload" HorizontalAlignment="Left" Margin="30,340,0,0" VerticalAlignment="Top" Width="75" IsEnabled="False" Click="ReloadButton_Click"/>
        <Button x:Name="ReloadFromButton" Content="Reload From..." HorizontalAlignment="Left" Margin="120,340,0,0" VerticalAlignment="Top" Width="100" IsEnabled="False" Click="ReloadFromButton_Click"/>
        <Button x:Name="RemoveButton" Content="Remove" HorizontalAlignment="Left" Margin="235,340,0,0" VerticalAlignment="Top" Width="75" IsEnabled="False" Click="RemoveButton_Click"/>
        <Button x:Name="OkButton" Content="OK" HorizontalAlignment="Right" Margin="0,0,179,21" VerticalAlignment="Bottom" Width="75" Command="ApplicationCommands.Close"/>
        <Button x:Name="CancelButton" Content="Cancel" HorizontalAlignment="Right" Margin="0,0,19,21" VerticalAlignment="Bottom" Width="75" IsCancel="True"/>
        <Button x:Name="ApplyButton" Content="Apply" Margin="0,0,99,21" IsEnabled="False" Height="20" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75"/>
    </Grid>
</Window>
